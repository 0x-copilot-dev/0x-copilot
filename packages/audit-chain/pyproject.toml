[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "enterprise-audit-chain"
version = "0.1.0"
description = "HMAC hash-chain signing/verification for tamper-evident audit logs, shared across enterprise-search services"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
