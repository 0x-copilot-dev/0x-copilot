// LibraryItemId lives in @enterprise-search/api-types — see Phase 0.5
// shared-primitives migration. The top-level chat-surface index
// re-exports it from the canonical site.
export {
  LibraryDestination,
  type LibraryItem,
  type LibraryItemKind,
} from "./LibraryDestination";
