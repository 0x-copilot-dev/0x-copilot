# Execution backlog — F1–F12 production integration

Living record of what the implementation program has found. Its purpose is to
let architectural decisions be made **deliberately and in one place**, rather
than as bandaids applied inside whichever lane happened to trip over the
problem.

**Nothing here is a work queue.** Items are not implemented because they are
listed. They are implemented when a human decides the architecture, and the
only standing exception is an item that blocks a step from activating at all —
those are marked as blockers and say so explicitly.

What belongs here:

- **Defects** — real bugs, whether live, latent, or pre-existing.
- **Optimizations** — measured opportunities, with the measurement.
- **Architectural smells** — duplication, misplaced authority, contracts that
  do not fit their adopters, redundancy that hides intent.
- **Library candidates** — work we wrote that an existing dependency could
  carry, and the honest assessment of whether swapping is worth it.
- **Already implemented elsewhere** — capability we built that the codebase or
  the framework already provides.

This is **not** the same list as
[`IMPLEMENTATION-BACKLOG.md`](./IMPLEMENTATION-BACKLOG.md). That file tracks the
ARQ items — the architectural gaps each F-series feature must close. This one
tracks what execution surfaced along the way, and is updated by the integration
owner as lanes land.

Nothing here is a live production defect unless the status says so. The F3 and
F6 code is dark: it is not composed into any running path yet. F8 is live, and
was changed only by a substitution with a proven parity property.

## 1. Defects

| ID       | Defect                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Found by                                    | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| BUG-01   | Subject-scope enforcement was silently skipped for the **default** `required_dimensions` — Pydantic does not validate defaults, so any policy built with defaults did not enforce subject scoping                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | RB.1                                        | **Fixed** `14260424` (`validate_default=True`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| BUG-02   | `ConcurrencyScope.UNKNOWN` reached `PermitScope._REQUIRED_COMPONENTS[kind]` and raised a bare `KeyError` instead of refusing conservatively. Latent — would have gone live the moment F6.2/F6.3 wired permits                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | F6.R                                        | **Fixed** `d489affa` (typed refusals; `UNKNOWN → GLOBAL`, the pool admitting least concurrency)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| BUG-03   | Lanes F3.2 and F3.3 merged with **zero textual conflict and 20 broken tests**: F3.2 made `task_policy_selection_ref` required, F3.3's tests were written against the old signature on different lines                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | root                                        | **Fixed** `4608367a`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| BUG-04   | Expanded capabilities carry `descriptor_revision = None`, because `McpLoadResult` does not surface the F8 revision. Expanded descriptors therefore cannot fold into `CapabilityCatalogGeneration`, so **F8 invalidation will not correctly fire for them** once expansion is enabled                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | F3.3                                        | **Open** — needs a `capabilities/mcp/` change no lane was allowed to make                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| BUG-05   | The shipped F8 post-I/O check is `A and B` where mutation testing shows **either half alone passes the entire suite** — mutually redundant in every reachable state. Pre-existing, not introduced; parity was preserved deliberately rather than silently simplified                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | RB.2                                        | **Open** — needs a deliberate decision, not a cleanup                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| BUG-06   | `test_factory_async.py::TestAsyncFactoryParallelism::test_total_latency_tracks_max_not_sum` asserts three 100 ms sleeps finish within ~220 ms. Fails under full-suite load, passes in isolation. Confirmed failing on an untouched base commit by two independent lanes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | F3.1, F6.R                                  | **Fixed** on main (PR #421, `bab88392`) — the test now pins listing overlap instead of wall-clock latency, which is the property it always meant to assert                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| BUG-07   | `search_capabilities`, `describe_capability`, and `invoke_capability` have **no entries in `builtin_operation_catalog.json`** and no operation descriptors. `test_every_assembled_model_tool_has_one_catalog_descriptor` stays green only because the inventory composes the dark path; it goes red the moment anything composes the deferred surface. Verified against the F-014 canary, not inferred                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | W1                                          | **Fixed** `1746cf4c` — F-014 canary goes from two/three unregistered capabilities to clean on both the factory and maximal deferred surfaces; the inventory proof now composes both postures with set equality, so a stranded catalog row fails as loudly as an uncatalogued tool                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| BUG-08   | **The mounted bridge can only return results it cannot dispatch.** `CapabilityBridgeRegistrar` mounts `CapabilitySearchTool` with a plain one-tier `DeterministicLexicalRanker`; F3.3's `TwoTierCapabilitySearch` is exported but **mounted nowhere in `src/`**. Dispatch bindings come from `RunScopedCapabilityDispatchBindings.from_disclosed`, built only from tier-two expansion output, so a catalog server-card ref has no binding and `invoke_capability` refuses with `capability_unavailable`. Every result the registered search can return is therefore describable but undispatchable. Not introduced by M-09 — already true of the server-card half — but M-09 removed the only entries that could ever have looked invokable, so it is now total. An integration gap between lanes F3.2 and F3.3: one built the two-tier search, the other mounted the one-tier ranker, and nothing joined them | M-09                                        | **Fixed** `9ddd0ae7` — one run-scoped join, not a second path: `dispatch.py` owns the dispatch vocabulary plus a `RunScopedCapabilityDisclosure` ledger of what this run's tier two disclosed, and `CapabilityCatalogAccess` resolves a ref across the immutable catalog _and_ that ledger. Closing test is a real search → describe → **invoke** chain on the same ref through the real gateway, with a standing negative control that pins the old behaviour so a silent regression cannot read as green                                                                                                                                                                                                                                                                                                               |
| SMELL-01 | **The Step-2 run-serial permit makes F6 parallelism unobservable.** `RunSerialAdmission` is an unconditional `asyncio.Lock` held around the _whole_ tool call (`capabilities/middleware/runtime_tool_control.py:397,426`). Per §8, F6 composes _inside_ it, so the effective width is `min(plan, permits, Step-2)` = **1 today, no matter what the plan says**. Every F6 lane so far — descriptor policy, permits, batch journal, coordinator — is correct and provably cannot produce an overlap in production. This is an architectural decision, not a bug: Step 2 deliberately installed one serial permit covering every graph-visible tool call, including framework-injected ones, and F6 was always meant to be what makes it selectively wider                                                                                                                                                        | F6.3                                        | **Fixed** `04d2a273` — a `ParallelAdmissionPort` injected into `RunSerialAdmission`. The cycle it avoids is real, not hypothetical: `capabilities/concurrency` already imports `control_plane`, so importing the coordinator the other way would close one, and a test pins that reverse edge to document _why_ the boundary exists. No bypass: the shared lane is a lightswitch over the **same** exclusive lock — first member acquires, last releases — so a serial call still waits behind a whole parallel group, proven by co-occurrence rather than by a maximum. **F6 remains dark until root wires it** (see W3)                                                                                                                                                                                                |
| BUG-10   | **`describe_capability` returned a _truncated_ schema, not an inline one.** Over-bound entries were trimmed to 32 parameters / 16 tags with a `metadata_truncated` flag, so the model would author arguments against a schema that is not the real one — the exact failure the artifact branch exists to prevent, already happening. Latent, since F3 is dark                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | F3.4                                        | **Fixed** `7dcc85c6` — parameters are now all-or-nothing: `schema_availability` is a closed `inline`/`artifact`/`unavailable` enum whose validator refuses hints on any non-inline answer, so **a partial schema is unrepresentable rather than merely avoided**. Intent tags are still trimmed, deliberately: a tag is a search cue, a parameter is the invocation contract                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| BUG-20   | **Every F6 journal append rescans the entire run event log.** `_append` calls `load_recovery_view` → `_replay` → `list_events_after(after_sequence=0)` **before every write** (`batch_journal_store.py:176`), and the scan covers _all_ run events, not only F6 ones. Cost is therefore quadratic in appends per run — one plan write per turn plus up to two child-transition writes per child. Measured by the lane at 20 appends → 230 event reads, growing by exactly one each time; the read path was verified at source by the integration owner                                                                                                                                                                                                                                                                                                                                                         | p95 lane (died mid-stream after finding it) | **Fixed** `f9fe6595`+`e7c55720` — and the defect was **two** full scans per append, not one: `_load_snapshot` → `EventJournalRunControlStore.get` also scanned from zero. Measured 72 appends: **8,100 envelope reads → 4**. Pinned by `cost(2N) <= 2 * cost(N)` over both adapters. Three safeguards were mutation-checked and **none had been load-bearing before**; the postgres adapter's `append_event` is lock-free, so the fold stops at a gap once primed                                                                                                                                                                                                                                                                                                                                                        |
| BUG-19   | **BUG-15's two authorities still do not meet in production.** `batch_concurrency_composition.py:424` is the only `RunBatchAdmission` construction site in `src/` and it passes no `approvals=`, so `GraphApprovalRequirementResolver.resolve(None, ...)` returns `None` and `ToolUsePolicySnapshot` — the run authority the fix's own docstring says the catalog author cannot see — is **never consulted**. The structural guarantee is real and correct; the wiring that feeds it is absent                                                                                                                                                                                                                                                                                                                                                                                                                  | Step 10 gate re-run                         | **Fixed** `afadb4cc`+`3de7addf` — the source asks the **production enforcer** (`ToolUsePolicyEnforcer.enforce`) whether a tool lands in `interrupt_on`, rather than modelling the policy's reach, so there is no second copy of the gated-tool table to drift. `runtime_context` is a required keyword with no default, so a composition root cannot silently reproduce the defect. Closing proof is deliberately a **pair**: `write=ask` → unplannable, simultaneity 1; `write=auto` → one parallel segment, simultaneity 3 — the gated half alone would pass just as well if the wiring had switched F6 off                                                                                                                                                                                                            |
| SMELL-08 | **`ProviderSessionConstraint` is an inert safety knob.** It is a parseable catalog dimension (`descriptor_policy.py:260`), it resolves and narrows correctly, it is journalled on `ConcurrencyPolicy` — and **nothing consumes it**. BUG-13's exact shape, inside the concurrency lane                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Step 10 gate re-run                         | Open — not blocking; fail-safe, since an unconsumed narrowing dimension cannot widen anything                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| SMELL-09 | **`rate_limit_scope` has zero consumers and descriptor precedence has one source.** Permit pools are a fixed ladder rather than a rate-limit coordinator, and only `PRODUCT_CATALOG` has a production source — nothing feeds `TRUSTED_PROVIDER` or `USER_APPROVED_OVERRIDE`, so two thirds of F6.1's precedence chain is unreachable                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Step 10 gate re-run                         | **Fixed** `239abfc3`+`8c7f9285` — a declared rate-limit scope now governs which pool an operation acquires from, and `TRUSTED_PROVIDER` has a production source in MCP tool annotations, honoured only when it narrows                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| BUG-22   | **A hunks operation's expected-result digest is never verified.** `WorkspacePatchSetMaterial._validate_present_result` checks the declared digest against `content_ref` for CREATE and REPLACE but **not for HUNKS**, so for a hunks operation the expected post-image digest is model-declared and unchecked. Latent only because nothing applies patches yet — it becomes live the moment Step 13's applier ships                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Step 13 planning                            | **Open — must be closed before Step 13's applier lands.** The plan's answer is to splice hunks during _validation_ rather than application, so the applier only ever handles precomputed content refs and "applied bytes differ from validated bytes" stops being representable                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| SMELL-10 | **The F1 operational corpus carries no real cost data, so no cost or latency claim can be made from it.** Every `FixtureUsage` is an arithmetic constant of `call_count` (`tokens = 100 × call_count`, `cost_microusd = 10 × call_count`); there are six distinct usage tuples across 43 families and the maximum `model_turns` anywhere is 3. The corpus proves _scorers_, not _economics_                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Step 11 entry gate                          | **Open — bears directly on Step 15**, whose promotion gates evaluate token, cost and latency outcomes over exactly this corpus. Those gates cannot be met from fixtures alone                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| BUG-21   | **The same quadratic scan exists in three more stores.** `EventJournalRunControlStore.get` (`api/run_control_store.py:62`) does a full `list_events_after(after_sequence=0)` on every call, and it is on the path of `model_invocation_store.py` (F10), `prompt_observation_store.py` (F2) and `task_policy_store.py` (F4) — each with its own `_validate_replay` and very likely the same shape. BUG-20 neutralised it for F6 by memoizing inside the F6 store; the other three are untouched                                                                                                                                                                                                                                                                                                                                                                                                                 | BUG-20                                      | Open — **the same bug three more times**, in `agent_runtime/api/`. Not blocking any step, but it is on every F2, F4 and F10 run                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| BUG-18   | **Cross-feature bleed in the trajectory projector corrupts F4 evaluation.** `TrajectoryProjector._policy_text` reads `payload["record"][key]` with **no discriminator**, so an F6 `operation_batch.journal.v1` event populates F4's `policy_record_kind` / `policy_disposition`. Verified live: an F6 plan projects as `policy_record_kind = "plan_bound"`, which is _also_ an F4 controller record kind that several F4 families assert on. **On a real run with F6 enforcing, an F6 batch plan could satisfy an F4 assertion.** Every other projector on that class is discriminated; only F4's is not                                                                                                                                                                                                                                                                                                       | F6 evaluation lane                          | **Fixed on main** `fed0b7a6` (PR #426, another agent) — the F4 policy projection is now gated on the tool-policy event, so an F6 batch-journal record can no longer populate F4's policy columns and satisfy an F4 assertion. Recorded here by the lane that found it; closed by someone else's change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| BUG-16   | **The consuming half of BUG-14 broke legacy replay.** Once four optional fields joined `QualityDecisionPayload`, `model_dump` began writing them as `null`, so every pre-extension journal row — which lacks the keys entirely — failed the store's mirror check and raised `RunControlJournalCorruption`. Introduced by `5aa75975`, verified on the untouched base by the next lane                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | BUG-14b                                     | **Fixed** `e292ab80` — absent and null are now the same statement, while a _disagreeing_ stored count still fails                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| BUG-15   | **Approvals have zero coverage in the F6 lane, and the safety argument for them is conventional rather than structural.** The implicit reasoning is "approval-gated work is effectful, effectful work is serial" — but nothing stops a `PRODUCT_CATALOG` entry declaring `side_effect=READ, mode=PARALLEL_SAFE` on a capability whose _gateway_ resolution requires approval, and the behaviour of N concurrent LangGraph `interrupt()`s inside one shared-lane cohort is unproven. No test in the concurrency lane mentions approvals                                                                                                                                                                                                                                                                                                                                                                         | F6.8 gate                                   | **Fixed** `d321ab66` — `ApprovalRequirement` is a narrowing-only dimension, so a later source can only make a capability _more_ approval-bound. Gated work is **not planned serially — it is not planned at all**: an approval is a suspend, and suspending N members of one admitted cohort opens N simultaneous human decisions where the serial path opens one. The lane also answered the question nobody had: **LangGraph cannot resume a multi-interrupt turn with one decision**, which is why unplannable is the right answer rather than serial. Recovered from a stalled lane and verified independently before merge                                                                                                                                                                                          |
| SMELL-05 | **F6.6's cancel and restart have no production route.** `BatchExecutionCoordinator.cancel` and `BatchRestartPlanner` are referenced by nothing outside `capabilities/concurrency/`. On cancel the wired path propagates `CancelledError` — it invents nothing, which the gate proved, but it also _records_ nothing, which is weaker than Step 10 work items 8 and 9 require. Pinned by two negative controls plus a positive control (`BatchExecutionCoordinator` **is** found outside the package) so they cannot pass vacuously                                                                                                                                                                                                                                                                                                                                                                             | F6.8 gate                                   | **Fixed** `b23ee9c3` — hooked into the existing queued `run_cancel_requested` handler rather than inventing a second cancellation mechanism. The hard part was that cancellation is fully out of band: nothing cancels the run's asyncio task, the handler runs as a _separate claim_, so the coroutine that learns a run is over is never the one executing it, while the coordinator is per-run and in-memory. A live-admission registry joins those two facts, and runs _before_ the status flip so admission stops earliest and the `indeterminate` rows land while the stream is still open. Restart refusal happens **before** the segment gate and before any dispatch intent is journalled, so refusing preserves the evidence it was derived from rather than downgrading a never-started read to indeterminate |
| SMELL-07 | **Two cancellation routes remain unrecorded, both deliberately.** Worker shutdown and host teardown raise `CancelledError` through the run handler, where a `RUNTIME_SHUTDOWN` reason would apply, but hooking it would be a _second_ place cancellation is handled and would need shielded awaits in a dying task. Separately, a cancel claim landing on a worker that is not executing the run finds no live coordinator and degrades to today's behaviour — a real distributed problem, tested rather than papered over (`test_a_run_this_process_is_not_executing_is_simply_missed`)                                                                                                                                                                                                                                                                                                                       | W4                                          | Open — both documented and tested; neither invents a rollback or a success                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| SMELL-06 | **The planner's `RESOURCE_CONFLICT` branch is unreachable from the graph.** `graph_admission.py:527-545` answers _unknown_ for any capability carrying a resource-key template, never a rendered fingerprint — so such capabilities never overlap at all. Production is therefore **safer** than planned, but the finer branch is dead by construction, the same shape as SMELL-02                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | F6.8 gate                                   | Open — both facts pinned. Per-subject overlap is not reachable until a resource key can actually be rendered at the seam                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| BUG-12   | **The Step RB revalidation cannot fire in production.** `build_capability_discovery_composer` supplies no `CatalogDescriptorRevisionSourcePort`, so the catalog generation folds **zero** descriptor revisions. Subject, connector scope, and task-policy selection are all frozen per run, so the live authority re-derives the _identical_ generation before and after descriptors move — a ref can never be reported stale mid-run. F3.2, F3.5, RB.2 and RB.3 all built and wired the revalidation correctly; it currently contributes nothing to the safety property it exists for. Verified at source by the integration owner                                                                                                                                                                                                                                                                            | F3.8 gate                                   | **Fixed** `c7884bd0` — the F8 resolver is projected over exactly the cards the run was already authorized to see, fixed when the catalog is projected, and re-awaited each time the live authority is asked. A source that discovered servers as the run opened them would have reported a different generation after the first search than before it, making every first-turn ref look stale. Root wired the in-process dev worker path separately. Proven end to end: search → describe → the backend's revision moves via the real `apply_notice` → invoke returns `CAPABILITY_STALE`, with the connector untouched so the refusal can only be the revision binding                                                                                                                                                   |
| BUG-13   | **The three `F3_DISCOVERY_*` expansion knobs are inert.** `CapabilityExpansionLimits.from_environment` has **no production call site** anywhere in `src/`; the composition root never sets `CapabilityBridgeComposition.expansion_limits`, the factory forwards the `None`, and the expander falls back to hard defaults. Effective K is always 3 regardless of `F3_DISCOVERY_MAX_EXPANDED_SERVERS`. Fail-safe rather than fail-open, but an operator knob that silently does nothing is worse than no knob                                                                                                                                                                                                                                                                                                                                                                                                    | F3.8 gate                                   | **Fixed** `c7884bd0` — measured through the seam: `F3_DISCOVERY_MAX_EXPANDED_SERVERS=5` over 9 matching servers opens 5, neither the default 3 nor the ceiling 8; `999` opens 3, not 8. A source-scan canary is kept, now pointing the other way — exactly one production reader, so one effective K                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| BUG-14   | **Two of the three F1 discovery cases cannot run on real events.** `quality.decision.v1` carries no numeric field, so a real-run projection reports recall rank 0, and both `selection_recall` and `end_to_end` declare `minimum_recall_rank: 1` — **a working system scores `capability_discovery_recall_missing`**. Only `unauthorized_probe` is real-run runnable, and its two numeric ceilings pass on _absent_ data rather than observed safety. Sharper than F3.7's own report, which described only recall as fixture-authored                                                                                                                                                                                                                                                                                                                                                                          | F3.8 gate                                   | **Fixed** `5aa75975` + `e292ab80`. The producer emits through a run-scoped `ref → position` correlator reached by identity behind a `weakref.finalize`, so no new argument had to reach the registrar; first offer wins, so a run cannot improve its score by re-searching, and refs never leave memory — proven by grepping a real run's durable output for the ref it correlated against. Counters are excluded from `digest_payload`, mutation-checked against a literal golden digest measured before the extension existed. **One residual:** an edited counter is not tamper-detected, which is inherent to that exclusion and asserted explicitly rather than left implicit                                                                                                                                       |
| SMELL-03 | **F6.5's `GatewayBatchChildExecutor` is not on the graph tool path.** W3 found that using it _alongside_ the framework's tool node would double-execute the connector, and using it _instead_ would mean suppressing the framework handler and re-synthesizing the `ToolMessage` — re-implementing ToolNode's result conversion, which is exactly the second path free to drift that the module's own docstring forbids. So the coordinator's runner closes over the framework body instead. The executor stays available for non-graph-initiated children (restart/recovery), but nothing calls it there either                                                                                                                                                                                                                                                                                               | W3                                          | Open — the lane's reasoning is sound and its indistinguishability proof still holds as a property; the question for root is whether ~580 lines and 39 tests should remain on a path nothing takes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| SMELL-04 | **The dark-path import standard is already unattainable for the F6 package.** `runtime_api/schemas/events.py:27` imports `capabilities.concurrency.batch_journal` for the batch event payload (added by F6.2's cross-package event work), so the package `__init__` runs in **every** deployment. W3's first draft re-exported its graph seam from that `__init__`, which would have put the admission seam on every import graph — its own parity test caught it, and the seam is now importable only by module path                                                                                                                                                                                                                                                                                                                                                                                          | W3                                          | Open — the F3 subsystem meets the stricter standard (a subprocess test proves it is never imported dark); F6 cannot, and the asymmetry is worth a deliberate decision rather than a surprise                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| SMELL-02 | **F3.4's schema-artifact branch is wired but unreachable.** `ExpandedCapabilityProjector` clamps to exactly the two bounds `schema_fits_inline` tests (32 params, 96 chars), so every tier-two record fits inline by construction, and a catalog member is a server card with no params. The publisher is threaded and correctly keyed — proven by re-derivation — but nothing a `deferred` run can describe today exercises it. Dead-by-construction rather than dead code                                                                                                                                                                                                                                                                                                                                                                                                                                    | W2                                          | Open — pinned by `test_no_expanded_capability_can_currently_reach_the_artifact_branch`, which fails the moment either projector bound is raised. That is the point at which to prove the branch end to end                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| BUG-11   | **The Step-2 run-serial permit uses two independent locks.** `RunSerialAdmission` holds a `threading.Lock` for `sync_permit()` and an `asyncio.Lock` for `async_permit()` (`control_plane/context.py:431-432`), so a synchronous graph tool call and an asynchronous one were never serialized against **each other** — the §8 guarantee of _one_ run-scoped permit covering every graph-visible tool call has a hole in it. Pre-existing since Step 2 and untouched by the SMELL-01 fix, which correctly declined to change behaviour outside its scope                                                                                                                                                                                                                                                                                                                                                       | SMELL-01                                    | **Open, latent, and now pinned.** The F6.8 gate proved it is **not reachable through the production graph**: the runtime drives the graph only via `ainvoke`/`astream`, and `ToolNode` picks its lane once per node invocation for a whole batch, so a turn is all-async or all-sync, never mixed. Critically, that containment is a **LangChain/LangGraph invariant, not ours** — `tool_node.py` calls the sync wrapper from inside `asyncio.gather` whenever it holds a sync wrapper and no async one, and that branch is dead only because `factory.py` uses the same predicate for both wrapper chains. Nothing in this repo owned that guarantee; it is now asserted against a really compiled graph, as a characterization test that goes red when BUG-11 is fixed                                                 |
| BUG-09   | **The `_display_title` override was already inert.** `_DisplayFields` declares an `InjectedToolCallId`, so LangChain rebuilds the model-facing schema through `_create_subset_model`, and that rebuild **drops aliases**. The model is therefore shown `display_title`, emits `display_title`, the worker records it verbatim — and `agent_display_from_payload` looked for `_display_title`, yielding `(None, None)`. A shipped feature that silently did nothing. Verified end-to-end against the real conversion                                                                                                                                                                                                                                                                                                                                                                                            | PERF-01                                     | **Open — awaiting decision.** A tested fix accepting both alias forms exists on the lane branch (`c25aa9ee`) and was deliberately left unmerged. Note it only reaches `event.presentation.title`, which `chat-surface` shadows behind `event.display_title`, so landing it end to end also needs `packages/chat-surface` or `runtime_api`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| GAP-01   | **No pinned proof asserted model-visible tool ordering** before W1. The three existing architecture assertions used `issubset`, `frozenset`, and set equality, so a reordering was invisible to CI                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | W1                                          | **Fixed** `4882f173` (pinned order tuple)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| GAP-02   | **F8's scope-keyed registry was pure round-trip work** — the authority wrote the caller's own trusted revision into a fingerprint-keyed dict and read it back one frame later. It existed only because the shared contract gave the authority no way to receive a resolution key, and the `forget` path existed only to bound its growth                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | RB.3                                        | **Fixed** `2d0ed56e` (registry deleted) + `9fd1a3d9` (inert `forget` and both call sites removed)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| RISK-01  | `McpServerCard.server_id` may contain colons, so a card with `server_id == "tool:cap_<32 hex>"` could in principle produce the expanded-identity shape. Unreachable in practice — those 32 hex chars are an HMAC output over the secret key, derived from the very catalog id the collision would need to occur under. Recorded rather than hardened, because changing identity strings is behavior change outside a zero-change lane                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | F3.M                                        | Open — cheap structural fix is refusing `:` in `source_id` at catalog-build time                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |

## 2. Missing or blocked

Work a step needs before it can activate, which no lane was scoped or permitted
to do.

| ID   | Missing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Blocks                            | Status                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| M-01 | `CapabilityCatalogGenerationPort` has a port and an authority adapter but **no production implementation**. The live generation source is whatever rebuilds the catalog per run                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | F3 activation                     | **Closed** `34fc378e` — one operator knob `F3_CAPABILITY_ACTIVATION` (absent → `direct`); the generation source holds no catalog and re-reads on every call, proven by walking closure cells and by a subprocess test asserting the discovery package never enters `sys.modules` on the dark path. **Root must still thread the MCP card snapshot** (async source vs sync dependency factory) or `deferred` composes nothing in production |
| M-02 | `runtime_worker/dependencies.py::_build_dependencies` sets neither `capability_activation` nor `capability_catalog`, so the factory has nothing to compose even in `deferred`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | F3 activation                     | **Closed** `34fc378e` — one operator knob `F3_CAPABILITY_ACTIVATION` (absent → `direct`); the generation source holds no catalog and re-reads on every call, proven by walking closure cells and by a subprocess test asserting the discovery package never enters `sys.modules` on the dark path. **Root must still thread the MCP card snapshot** (async source vs sync dependency factory) or `deferred` composes nothing in production |
| M-03 | The `executor` and `revalidation` seams are unthreaded, so `invoke_capability` can never register                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | F3 invoke path                    | **Closed** — `e706301d` builds `GatewayCapabilityExecutor`; root threads it in the factory                                                                                                                                                                                                                                                                                                                                                 |
| M-04 | Two `RevisionBoundRef` contract defects, hit independently by both adopters: `run_id` is mandatory even for adopters with no run, and the one-way fingerprint forces every adopter into a scope-keyed registry it must lifetime-manage                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | F5, F9, F11 adoption              | **Closed** — `2d0ed56e`; F8's registry deleted outright, F3 never needed one                                                                                                                                                                                                                                                                                                                                                               |
| M-05 | The shared revalidation primitive has no vocabulary for (a) bounded local staleness, (b) "binding current, material gone", or (c) a generation barrier over material that has no revision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Steps 9, 12, 13                   | Open — recorded on the RB.2 lane bullet                                                                                                                                                                                                                                                                                                                                                                                                    |
| M-06 | `HmacCapabilityReferenceMinter` duplicates `AuthorizedCatalogBuilder._opaque_id` — same HMAC-SHA256, two implementations, two keys                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | F3 correctness hygiene            | **Closed** — `274e867f`                                                                                                                                                                                                                                                                                                                                                                                                                    |
| M-07 | F3.3's `expansion_trigger_candidates` heuristic ("tier one returned fewer than 3 tool cards") is invented. The PRD says "when a query cannot be satisfied confidently" without defining it. **Note:** M-09's ruling removes tool cards from the bridge catalog, so this heuristic counts something that can no longer exist — the M-09 lane must leave it coherent even though the product question stays open                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | F3.8 cohort matrix                | Open — needs a product call                                                                                                                                                                                                                                                                                                                                                                                                                |
| M-13 | **`deferred` mode suppresses nothing.** The bridge tools are appended and neither the MCP card block (prompt fragment 20) nor the direct MCP tools are removed, so F3 today makes the prompt _larger_ and is net-negative on its own stated purpose (§11 Step 8: "reduce prompt/tool-schema load"). The card block is one unbounded line per authorized server — roughly 1,000–1,500 tokens at 20 connectors, paid every turn and linear in connector count — and it is the only part of F3 that touches prompt size at all. Step 8's "direct/server fallback remains available" means switching activation modes, not registering both paths at once; W1 already built the safe degradation, so suppression need only be conditional on the bridge having actually registered                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **F3's entire value proposition** | **Closed** `60e3a577` (lane F3.9) — the deferred prompt is now flat at 853 tokens for any connector count, proven byte-identical across 0, 1, 20 and 60 servers                                                                                                                                                                                                                                                                            |
| M-08 | **`idempotency_key` has no gateway seam.** `McpToolCallRequest` carries no idempotency field into the `OperationInvocationRegistry` coalescer, so a bridge invoke refuses it rather than promising at-most-once for an external effect and not delivering. Fix belongs to `mcp/cards.py` + `mcp/middleware/call_tool.py`: an optional passthrough into `OperationRequestFactory.create(operation_id=...)`, which already accepts one                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | F3 invoke completeness            | Open — fails closed today, which is correct                                                                                                                                                                                                                                                                                                                                                                                                |
| M-09 | **Tool-card capabilities are undispatchable.** The catalog holds both `MCP_SERVER` and `TOOL_CARD` entries, but only the former can be dispatched: `CallMcpTool` is a real non-model seam into the Operation Gateway, and there is no product-tool equivalent (`LangChainToolDispatcher` takes LangChain tools, so using it would mean a second dispatch path — which F3.5 correctly refused to build). `executor.py:458` refuses any non-MCP source with `capability_unavailable`. **Consequence:** in `deferred` mode the model can search, find, and describe a product tool card, then be refused on invoke — the bridge advertises what it cannot deliver. Two closes, and it is a product call: **(a)** build a non-model dispatcher for tool cards, or **(b)** keep `TOOL_CARD` entries out of the _searchable_ catalog so the model never sees an uninvokable entry, leaving product tools directly registered as they are today. **RULED: (b), implemented structurally** — the builder must refuse tool cards for a bridge catalog rather than filter them, so a future caller cannot reintroduce the dead end. Explicitly **not** a token decision: the catalog is a run-side artifact and is never injected into the prompt, so this changes prompt size by exactly zero. Rationale — F3 exists to cut MCP schema bulk from the prompt, and the product's own tool set is small, stable, and already registered. Not urgent: F3 is dark, and `direct`/`server` register product tools normally regardless | F3 coverage                       | **Closed** `42567cc0` — structural in two layers: `CapabilitySource.has_non_model_dispatch` derives the admissible set by iterating the closed enum, and `CapabilityIndexEntry` refuses an inadmissible source at the single membership chokepoint. `build()` lost its `tool_cards` parameter outright, so a future caller gets a `TypeError` rather than a silent drop                                                                    |
| M-10 | **`tool_call_id` is not threaded into `CapabilityInvokeRequest`**, so bridge-dispatched operations bind citations at trace granularity via the documented `trace_id` fallback rather than per call                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Citation precision                | Open                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| M-11 | `discovery/__init__.py` does not re-export the executor symbols, and F3.M left deliberate compatibility re-exports in `ranker.py`/`expansion.py` for symbols it moved to `contracts.py`. Both are one-round artifacts of concurrent lanes; drop the re-exports and add the executor exports now that the round is merged                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | F3.4 import ergonomics            | Open — mechanical                                                                                                                                                                                                                                                                                                                                                                                                                          |
| M-12 | `ExpandedCapability` has no `schema_digest`, so `RunScopedCapabilityDispatchBindings.from_disclosed` needs `(ref, server_name, descriptor)` triples instead of building from the expansion result alone                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | F3 wiring ergonomics              | **Closed** `9ddd0ae7` — `ExpandedCapability` carries a required `schema_digest`, so bindings build from the expansion result alone; the derivation is stated once                                                                                                                                                                                                                                                                          |

## 3. Proportionality and library reuse

Places where what we built may exceed what the problem needs. Recorded so the
question is answered once, with evidence, rather than relitigated per lane.

| ID   | Subject                                        | Assessment                                                                                                                                                                                                                                                                                           | Recommendation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ---- | ---------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| L-01 | `concurrency/permits.py` (396 lines)           | `anyio.CapacityLimiter` is already in the dependency tree and covers **single-scope** capacity. What no library provides is **atomic multi-scope acquisition** — a call needing global AND connector AND capability permits at once without deadlock or starvation — and that is most of the file    | **Do not rewrite.** A library removes maybe 100 lines and adds real risk to merged, mutation-verified code                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| L-02 | `concurrency/kill_switches.py` (653 lines)     | Conceptually: read operator config → fold to the narrowest value → report which switch narrowed it. It is **larger than `descriptor_policy.py` (444)**, which solves the genuinely harder problem. Three independent anti-widening layers on a two-field value object is more than the problem needs | **Assessed and declined — not scheduled.** The size is real but the redundancy is the point: this is the emergency control, and its three layers make widening unrepresentable, structurally impossible to compose wrongly, and refused at the contract even if a future resolver is buggy. Collapsing to one layer trades a defence-in-depth property for ~300 lines of tested code. Revisit only if it becomes hard to change, not because it is long. F6.2 has since reused its narrowing validator as the batch record's own no-broadening proof, so the surface is earning its keep |
| L-03 | LangGraph tool concurrency                     | `ToolNode` runs tool calls concurrently and `RunnableConfig` exposes `max_concurrency`. That is one global integer with no notion of which operations may overlap — it cannot express "two writes to the same page must serialize". PRD §6.6 already rejects it as the safety boundary               | **Settled: do not adopt.** Recorded here so it is not reopened                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| L-04 | `concurrency/descriptor_policy.py` (444 lines) | Domain policy: which of _this product's_ connector operations are safe to overlap, and how a provider claim narrows a catalog claim. No library can supply this                                                                                                                                      | **Irreducible.** Leave alone                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| L-05 | `concurrency/contracts.py` (1,312 lines)       | Pydantic contract surface, mandated by the service's own engineering rules (typed boundaries, closed enums, no `dict[str, Any]` domain state)                                                                                                                                                        | **Leave alone.** Size here is the house style, not overbuild                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |

## 4. Process findings

Worth keeping because they change how the remaining waves should be run.

- **A clean three-way merge is not evidence of a correct merge.** BUG-03 produced
  zero conflicts and twenty failures. The integration owner runs the affected
  suite after _every_ lane merge, not once per round.
- **Isolated concurrent lanes necessarily duplicate types.** Barring lanes from
  each other's files is what makes parallelism safe, and the cost is a
  reconciliation lane afterwards (F6.R, F3.M). Budget one per module per round
  rather than treating it as rework.
- **Agent worktrees are created from `main`, not from the integration branch.**
  Every lane prompt must pin its base commit explicitly, or lanes silently build
  on stale code. Round 1 survived this only because it depended on nothing
  unmerged.
- **A new event family is inherently cross-package, so a lane adding one needs
  `packages/api-types` and `runtime_api/schemas/` in its write list.** PRD §14
  requires the Python enum, payload validation, persistence, projection, and the
  TypeScript discriminated union to move together, and
  `test_typescript_runtime_event_constants_match_backend_enums` asserts
  set-equality between them — so a write-list that omits them makes the
  requirement unsatisfiable. Lane F6.2 exceeded its stated list for exactly this
  reason, disclosed it prominently, kept every edit append-only (173 insertions,
  0 deletions), and ran the TypeScript typecheck. The fault was in the brief,
  not the lane.
- **A lane briefly wrote to the main checkout instead of its worktree**, caught
  it, reverted with an exact reverse edit, and verified byte-identity. The
  integration owner confirmed the main checkout was clean independently rather
  than taking the report at face value. Keep verifying this claim specifically —
  a stray commit on the main checkout is the one subagent failure mode that is
  expensive to undo.
- **The most valuable lane output is where a shared contract did not fit.**
  RB.2's six adoption findings are worth more than its code, because they price
  the next three adoptions. Ask for that explicitly in lane briefs.

## 5. Measured token economics

Lane F3.9 measured the real numbers through the production factory with real
`McpServerCard`s and `cl100k_base` counting. They are recorded here because the
program had been reasoning from an integration-owner hand estimate that was
wrong by roughly 2–3×.

| Servers | Prompt, `direct` | Prompt, `deferred` | Prompt saved |
| ------- | ---------------- | ------------------ | ------------ |
| 0       | 752              | 853                | −101         |
| 10      | 1,100            | 853                | 247          |
| **20**  | **1,389**        | **853**            | **+536**     |
| 30      | 1,715            | 853                | 862          |
| 40      | 2,044            | 853                | 1,191        |

The `deferred` prompt is **flat at 853 tokens for any connector count** — the
replacement block is one fixed paragraph, proven byte-identical across 0, 1, 20,
and 60 servers.

**The estimate that scheduled lane F3.9 was wrong.** Root projected 1,000–1,500
tokens saved at 20 connectors; the measured figure is **536**, about 29 tokens
per server. The lane was still worth doing — the saving is real and it makes the
prompt flat rather than linear — but the decision was made on a number roughly
2.5× too optimistic, and that is worth remembering the next time a lane is
prioritised on an unmeasured estimate.

### PERF-01 — the largest token lever found, and it is not F3

The three F3 bridge tool schemas cost **830 tokens** in the provider tool
payload, which is why F3 remains **net-negative below roughly 28–30
connectors** even after F3.9. Investigating that surfaced something larger.

`wrap_tools_with_display` adds three properties — `_display_title`,
`_display_summary`, and `tool_call_id` — to **every** model tool's
`args_schema`. Measured directly by the integration owner: a bare probe tool
costs 54 tokens; the same tool decorated costs **327**. That is **273 tokens of
identical schema boilerplate per tool**, and it is repeated verbatim on all 14
tools of the deferred surface — roughly **3,800 tokens on every turn**.

For scale: that is **seven times** F3's entire prompt saving at 20 connectors,
and larger than the whole MCP card block even at 40 connectors. It has nothing
to do with F3 and would be paid identically with F3 removed.

The properties are identical on every tool, so the fix direction is to state the
convention once rather than fourteen times — either by shortening the field
descriptions or by moving the prose into the system prompt and keeping the
schema fields terse. This needs its own measurement and a check that the model
still populates the fields correctly; it is not a change to make blind.

**Recommendation: this outranks F3.6/F3.7 and probably outranks finishing F3.**
