# Settings Parity & Wiring — status

**Branch:** `claude/0xcopilot-settings-parity-2654f3` (kept in sync with `main`).
**Scope:** the three **Models & keys** settings sections — Provider keys, Local models, Model & behavior — to full v3-design parity + real backend wiring, converged across web & desktop.
**Predecessor:** [frontend-parity-v3](../frontend-parity-v3/README.md) (PRD-E settings convergence, PRD-F provider keys).

## PRDs

| PRD | Section                                                           | Doc                                                            |
| --- | ----------------------------------------------------------------- | -------------------------------------------------------------- |
| 00  | Program overview (architecture, tokens, NFR platform, sequencing) | [PRD-00-overview.md](./PRD-00-overview.md)                     |
| 01  | Provider keys (BYOK)                                              | [PRD-01-provider-keys.md](./PRD-01-provider-keys.md)           |
| 02  | Local models (Ollama)                                             | [PRD-02-local-models.md](./PRD-02-local-models.md)             |
| 03  | Model & behavior                                                  | [PRD-03-model-and-behavior.md](./PRD-03-model-and-behavior.md) |

## Locked decisions (no-bandaid defaults; PRD-00 §7)

| #   | Decision                                     | Locked choice                                                                                                                       |
| --- | -------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| D-1 | Per-provider default model                   | **Real per-provider default** — add `ProviderKeySummary.default_model` + store column; stop clobbering the single workspace default |
| D-2 | Custom OpenAI-compatible endpoint            | **Real add-a-custom-endpoint flow** (base-URL + key) over the existing OpenAI-compatible registry                                   |
| D-3 | "Default local model" home                   | **`workspace_defaults`** (beside `default_model`, the run-resolution source)                                                        |
| D-4 | Available-models catalog                     | **Curated static list** (api-types) + **free-text advanced path** (keep power-user flow)                                            |
| D-5 | Reasoning-depth vocabulary                   | **Canonical `auto/quick/standard/deep`** at UI/settings; map to runtime `fast/balanced/deep`                                        |
| D-6 | Web access                                   | **Real tool-gate** — capability classification + run-context filter (load-time), not UI-only                                        |
| D-7 | Default-model / reasoning / web-access scope | **Org-scoped `workspace_defaults`** for the solo profile (org==user)                                                                |

## Legend

✅ shipped (green: typecheck + tests) · 🟡 in progress · ⬜ not started

## Waves & slices

| Wave / slice | Summary                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Status |
| ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| **A1**       | Token/type parity: added `--color-accent-line` + missing `--color-danger-contrast`/`--color-success-contrast` (3 themes) to the SoT; purged 56 stale atlas-orange `var()` fallbacks in settings pages. Global base-size deferred (see note).                                                                                                                                                                                                                                                                                                                                                                                                 | ✅     |
| **B1+B2**    | Per-provider default model. PR-F.5 landed on main since discovery (migration 0042 + backend + read-projection) but left the client write-path stale. Root fix: `port.save` now persists per-provider `default_model` in the same PUT → `summary.default_model` is server-projected on both hosts and the row chip survives reload; `modelChips` made symmetric across hosts as the legacy fallback (closes the divergence at the root).                                                                                                                                                                                                      | ✅     |
| **B3**       | Provider keys polish. Keychain-copy ✅ (on main). Real colored brand logos ✅ (shipped in the cosmetic-parity PR — OpenAI/Anthropic/OpenRouter tiles + Google 4-color G). **Remaining:** the real custom OpenAI-compatible-endpoint add-flow (base-URL + key + SSRF guard, decision D-2) — the biggest remaining B item.                                                                                                                                                                                                                                                                                                                     | 🟡     |
| **C1+C3**    | `createLocalModelsPort(transport)` (substrate-agnostic: `request` + `subscribeServerSentEvents`); desktop `SettingsMount` + web `SettingsBinder` both mount `LocalModelsPage` with **identical** wiring (real status/list/pull-SSE/delete + curated `LOCAL_MODEL_CATALOG`); legacy web `sections/LocalModels.tsx` retired. `DownloadLocalModelModal` gains a free-text custom repo/quant path (no download regression) + size-probe. Verified: 3 typechecks clean · chat-surface settings 261 · desktop 1003 · eslint substrate-clean · 7 FE failures identical on clean base (pre-existing jsdom flakes).                                   | ✅     |
| **C2**       | Default-local-model persistence. `default_local_model` added to `workspace_defaults.behavior_overrides` (validated, additive JSONB — no migration); both binders read-merge-write it (lockstep) so the "default local" chip + Set-default + download's "use as default" toggle round-trip on both hosts (honest error toast, no fake success). Salvaged from a stalled agent + finished (web mount + a null-safe `behavior_overrides?.` crash fix). Verified: FE+desktop typecheck 0 err · desktop 1020 · chat-surface 264 · ai-backend workspace 52. _Dedicated field round-trip test = light follow-up._                                   | ✅     |
| **D1**       | Reasoning-depth ✅ (PR #201). Canonical `default_reasoning_depth` (fast/balanced/deep, null=Auto) on `workspace_defaults.behavior_overrides` + api-types; a before-validator reconciles the legacy `default_reasoning_effort`; `RunCoordinator` seeds a run that omits depth from the persisted default → `DepthBudgetTable` scaling; `ModelBehaviorPage` (auto/quick/standard/deep) + desktop binder wired.                                                                                                                                                                                                                                 | ✅     |
| **D2**       | Approval-policy **runtime enforcement** ✅. `ToolUsePolicyGate.decide()` had zero runtime callers; now `tool_use_enforcement.py` (`ToolUsePolicyResolver` + `ToolUsePolicyEnforcer`) is wired into `factory._assemble_harness`: builds the snapshot from `user_policies_json['tool_use']`, **fails open** to deployment defaults (read=auto/write=ask/destructive=require), routes ask/require → the existing HITL interrupt, block → a safe-message `PolicyBlockedTool`, auto → untouched. Conservatively scoped to `call_mcp_tool`. Verified: ai-backend unit 3061 pass (14 new). _Desktop `ApprovalPolicy`→storage wiring folds into D5._ | ✅     |
| **D3**       | Web access ✅ (PR #201). `web_access_default` on `behavior_overrides` + tri-state `CreateRunRequest.web_search_enabled`; `RunCoordinator` seeds the flag from the persisted default when the request omits it (the runtime `WebSearchToolRegistry` gate already existed); `ModelBehaviorPage` toggle + desktop binder wired.                                                                                                                                                                                                                                                                                                                 | ✅     |
| **D4**       | Spend-cap ✅ (PR #201). `SpendGuardrailPort` → `/v1/budgets` (read caller's monthly `scope=user` budget; upsert POST/PATCH/DELETE; float↔micro-USD in the port); desktop wired (honest load/save, no fake $0). **Security fix:** org-scoped/other-user budget writes require `admin:budgets` and 403 **regardless of `RBAC_MODE`**; `/v1/budgets/me` caller-scoped. _Follow-up: grant `admin:budgets` to `role_system_admin` (backend migration) for org-scoped team budgets under enforce._                                                                                                                                                 | ✅     |
| **D5**       | Web-convergence capstone — **remaining.** Mount the shared `ModelBehaviorPage` on web (retire legacy `ModelAndBehavior`, complete the reasoning-depth rename), wire desktop `ApprovalPolicy`→`/v1/me/policies/tool-use`, add the slug↔page invariant test, retire dead provider-keys web code. Depends on D1/D3/D4 (now done).                                                                                                                                                                                                                                                                                                               | ⬜     |
| **litellm**  | Model names + pricing + token counting all sourced from the litellm library (installs clean — the old openai/pydantic conflict was stale). Slice-1 pricing (PR #194) + slice-2 catalog (PR #194) + slice-3 tokens (PR #200), all ✅ merged. Fixed a 3–10× over-billing bug (stale seed placeholders). Plan: `services/ai-backend/docs/refactor/01-pricing-from-litellm.md`.                                                                                                                                                                                                                                                                  | ✅     |

**Sequencing refinement (principal-eng review).** The original PRD-00 §6 "A2 — big-bang web convergence" was split into the section verticals to avoid a regressed half-wired intermediate: web converges onto `LocalModelsPage` **inside C1** (once the port + real wiring exist) and onto `ModelBehaviorPage` **inside D5** (once D1–D4 wire the knobs, so web never loses its currently-wired `ToolUsePolicyPanel`). Dead provider-keys web code is retired in D5.

**Deferred (out of this program).** Pinning the global base body font-size to the design's ~13px "quiet" size is a genuine, documented drift but is **app-wide** (affects chat, activity, every surface) and **orthogonal to these three settings sections** (which set explicit `--font-size-*` on every text node). It needs cross-surface visual verification and should land as its own verified slice, not a blind global change here.

## Commit trail

- `7f490fac` docs(settings-parity): PRD suite (PRD-00..03 + STATUS)
- `048ed69b` **A1** feat(design-system,chat-surface): accent-line + contrast tokens; purge stale atlas-orange fallbacks — merged via PR #183
- `15c7c221` **B1+B2** feat(provider-keys): persist per-provider default_model on save; symmetric modelChips — merged via PR #187
- `fe30e841` **C1+C3** feat(local-models): LocalModelsPort + both hosts mount LocalModelsPage; retire legacy web section; free-text download path — merged via PR #188
- `e0d92e99` **D2** feat(ai-backend): enforce per-user tool-use approval policy at run-start (fail-open, reuse HITL interrupt, block via safe wrapper)
