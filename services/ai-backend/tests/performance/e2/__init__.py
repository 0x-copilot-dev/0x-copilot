"""Deterministic E2 capacity gates.

These tests intentionally measure bounded work, not hosted-runner speed.  The
reporting command may record elapsed time, but pass/fail is driven by the
versioned operation limits in ``tools/e2-performance/limits.json``.
"""
