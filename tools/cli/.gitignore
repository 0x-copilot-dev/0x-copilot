# Generated by scripts/assemble-payload.mjs at prepack — a mirror of the
# monorepo subset the runtime needs, plus the built desktop app. Multi-MB,
# reproducible, never committed.
payload/

node_modules/
*.tgz
